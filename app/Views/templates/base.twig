{# app/Views/templates/base.twig - Základní layout pro všechny stránky #}
<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Food App{% endblock %}</title>

    {# Bootstrap 5 CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    {# Vlastní CSS #}
    {% block extra_css %}{% endblock %}
</head>
<body class="bg-light">

    {# Navigační lišta - zobrazí se když je uživatel přihlášený #}
    {% if session.user_id is defined %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="?page=home">🍕 Food App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <span class="navbar-text text-white me-3">
                            Přihlášen: <strong>{{ session.jmeno }}</strong> ({{ session.role }})
                        </span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="?page=logout">Odhlásit se</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    {% endif %}

    {# Hlavní obsah stránky - každá šablona si ho naplní po svém #}
    <main class="container mt-5">
        {% block content %}{% endblock %}
    </main>

    {# Bootstrap JS #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    {# Vlastní JavaScript #}
    {% block extra_js %}{% endblock %}
</body>
</html>
